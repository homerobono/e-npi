import { FileButtonDirective } from './directives/file-button.directive';

describe('FileButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FileButtonDirective();
    expect(directive).toBeTruthy();
  });
});
