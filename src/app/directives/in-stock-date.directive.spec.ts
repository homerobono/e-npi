import { InStockDateDirective } from './in-stock-date.directive';

describe('InStockDateDirective', () => {
  it('should create an instance', () => {
    const directive = new InStockDateDirective();
    expect(directive).toBeTruthy();
  });
});
